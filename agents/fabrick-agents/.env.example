# Standalone-mode fallback. When coral-server spawns this agent it injects
# CORAL_PROXY_URL_MAIN / CORAL_PROXY_MODEL_MAIN automatically and this key
# isn't read — the proxy handles upstream auth at the server level.
OPENROUTER_API_KEY=
