node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Editors
.vscode
.idea
*.swp
*.swo

# Playwright (MCP session snapshots + screenshot output)
.playwright-mcp
.playwright-artifacts
playwright-report
test-results

# Spike scratch data (throwaway scripts under scripts/)
scripts/.spike-data/
scripts/**/*.db
scripts/**/*.db-journal

# Local / throwaway scripts — ad-hoc setup + testing harnesses (Privy signing
# ladder, policy setup, etc.). Real tooling (dev-stack, migrate-turso) stays
# tracked under scripts/. Put anything you don't want committed in here.
scripts/local/
