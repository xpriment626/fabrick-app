{
  "name": "fleet-gateway",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Public edge for the Coral fleet runtime — token custody, per-session ownership, event-stream relay. Built Docker-free; ports to a container later unchanged.",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "start:node": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
